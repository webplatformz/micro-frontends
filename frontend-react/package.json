{
  "name": "frontend-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "http-server": "0.11.1",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "serve": "http-server build -p 3000 --cors",
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom",
    "eject": "react-scripts eject",
    "watch": "PUBLIC_URL=http://localhost:3000/ nodemon --watch src --exec 'npm run build'"
  },
  "devDependencies": {
    "babel-plugin-transform-custom-element-classes": "0.1.0",
    "nodemon": "^1.17.5",
    "react-app-rewired": "1.5.2"
  }
}
